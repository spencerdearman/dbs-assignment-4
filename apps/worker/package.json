{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "description": "Railway worker for polling Open-Meteo and writing weather updates to Supabase.",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch --env-file=.env index.js",
    "start": "sh -c 'if [ -f .env ]; then node --env-file=.env index.js; else node index.js; fi'"
  },
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^2.104.0"
  }
}
